[package]
name = "gitsail-protocol"
version = "0.0.0"
edition.workspace = true

[dependencies]
gitsail-domain = { path = "../gitsail-domain" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
